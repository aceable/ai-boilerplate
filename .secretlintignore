# Build output — contains vendored third-party source (e.g. snowflake-sdk private key patterns)
.next/

# Skill reference docs — contain example/placeholder credentials, not real secrets
.claude/skills/
