{
    "$schema": "https://railway.app/railway.schema.json",
    "build": {
        "buildCommand": "npm run build"
    },
    "deploy": {
        "startCommand": "if [ -n \"$DATABASE_URL\" ]; then npm run db:migrate; else echo 'DATABASE_URL not set — skipping migrations'; fi && next start -p $PORT",
        "healthcheckPath": "/api/health",
        "healthcheckTimeout": 100
    }
}
